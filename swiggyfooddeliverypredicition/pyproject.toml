[project]
name = "swiggyfooddeliveryprediction"
version = "0.0.0"
dependencies = [
    "Flask",
    "pandas",
    "joblib",
    "scikit-learn",
    "numpy"
]

[project.scripts]
app = "backend.app:app"