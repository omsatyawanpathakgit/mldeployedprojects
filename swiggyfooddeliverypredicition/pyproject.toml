[project]
name = "swiggyfooddeliveryprediction"
version = "0.0.0"
requires-python = ">=3.12,<3.13"

dependencies = [
    "flask",
    "pandas",
    "joblib",
    "scikit-learn==1.6.1",
    "numpy"
]

[project.scripts]
app = "api.app:app"