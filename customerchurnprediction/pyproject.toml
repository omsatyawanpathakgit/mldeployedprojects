[project]
name = "customerchurnprediction"
version = "0.0.0"
requires-python = ">=3.12,<3.13"

dependencies = [
    "flask",
    "pandas==2.3.0",
    "numpy==2.3.1",
    "joblib==1.5.1",
    "scikit-learn==1.6.1",
    "imbalanced-learn"
]

[project.scripts]
app = "api.app:app"